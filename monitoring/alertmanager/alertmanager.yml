global:
  resolve_timeout: 5m

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'default'

receivers:
- name: 'default'
# - name: 'telegram'
#   telegram_configs:
#   - bot_token: 'YOUR_BOT_TOKEN' # Replace with your token
#     chat_id: 0                # Replace with your chat_id (integer)
#     send_resolved: true
#     parse_mode: 'HTML'
#     message: '<b>{{ .Status | toUpper }}</b>: {{ .CommonAnnotations.summary }}\n{{ .CommonAnnotations.description }}'
